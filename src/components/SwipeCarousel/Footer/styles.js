import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    position: 'fixed',
    bottom: 150,
    width: 170,
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'space-around',
    zIndex: -1,
  },
});
